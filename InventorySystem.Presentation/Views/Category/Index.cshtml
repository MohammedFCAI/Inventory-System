@model IEnumerable<Category>
@* @{
    ViewData["Title"] = "Index";
}

<div class="row">
    <div class="col-6"> 
        <h1>All Categories</h1>
    </div>
    <div class="col-6">
        <a asp-controller="Category" asp-action="Create" class="btn  btn-primary topright"><i class="bi bi-plus-circle-dotted"></i> Create New Category</a>
    </div>
</div>

<br />

 @if (Model.Count() > 0)
 {
     <table class="mt-4 table table-striped table-hover">
         <thead>
             <tr class="text-center">
                 <th>Number</th>
                 <th>Name</th>
                 <th>Details</th>
                 <th>Update</th>
                 <th>Delete</th>
             </tr>
         </thead>

         <tbody>
             @for (int i = 0; i < Model.Count(); i++)
             {
                 var category = Model.ElementAt(i);
                 <tr class="text-center">
                     <td>@(i + 1)</td>
                     <td>@category.Name</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@category.Id" class="btn btn-warning">
                            <i class="bi bi-eye-fill"></i>
                            Details
                        </a>
                    </td>
                     <td>
                         <a asp-action="Edit" asp-route-id="@category.Id" class="btn btn-info">
                             <i class="bi bi-pencil-square"></i>
                             Edit
                         </a>
                     </td>
                     <td>
                         <a asp-action="Delete" asp-route-id="@category.Id" class="btn btn-danger"
                            onclick="return confirm('Are you sure to delete this category?');">
                             <i class="bi bi-trash"></i>
                             Delete
                         </a>
                     </td>
                 </tr>
             }
         </tbody>
     </table>
 }
 else
 {
     <div class="mt-4 alert alert-warning">
         <h4>There is no categories!</h4>
     </div>
 } *@



@{
    ViewData["Title"] = "Categories List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Bootstrap CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.datatables.net/2.1.8/css/dataTables.bootstrap5.min.css">
<link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">

<style>
    body {
        font-family: 'Nunito', sans-serif;
    }

    .add-category-container button {
        background-color: #4E73DF;
        color: #fff;
        border: none;
        padding: 10px 20px;
        cursor: pointer;
        border-radius: 4px;
        font-size: 1rem;
        margin-bottom: 20px;
    }

        .add-category-container button:hover {
            background-color: #0056b3;
        }

    .modal-footer .btn-delete {
        background-color: #dc3545;
        color: #fff;
        border: none;
    }

    .modal-footer .btn-cancel {
        background-color: #d0e7f5;
        color: #333;
        border: none;
    }

    .table {
        border-radius: 8px;
        overflow: hidden;
    }

        .table th, .table td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
        }

        .table thead {
            background-color: #4E73DF;
            color: white;
        }

        .table tr:nth-child(even) {
            background-color: #f8f9fa;
        }

        .table tr:hover {
            background-color: #f1f1f1;
        }

    .btn {
        border-radius: 4px;
    }

    .btn-primary {
        background-color: #4E73DF;
        border: none;
        transition: background-color 0.3s;
    }

    .btn-danger {
        border-radius: 4px;
        transition: background-color 0.3s;
    }

    .btn-primary:hover {
        background-color: #375a9c;
    }

    .btn-danger:hover {
        background-color: #c82333;
    }
</style>

<div class="add-category-container">
    <button onclick="window.location.href='@Url.Action("Create", "Category")'">Add New Category</button>
</div>


@if(Model.Count() > 0)
{
    <div class="table-container">
        <table class="mt-5 table table-striped table-hover table-responsive align-middle table-light p-3" id="categoryTable">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var cateogry in Model)
                {
                    <tr>
                        <td>@cateogry.Id</td>
                        <td>@cateogry.Name</td>
                        <td>
                            <a class="btn btn-sm btn-primary" href="@Url.Action("Edit", "Category", new { id = cateogry.Id })">
                                <i class="fas fa-pen ml-1"></i>Edit
                            </a>
                        </td>

                        <td>
                            <button type="button" class="btn btn-sm btn-danger" onclick="confirmDelete(@cateogry.Id, '@cateogry.Name')">
                                <i class="fas fa-trash-alt"></i> Remove
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
} 
else
{
    <div class="mt-4 alert alert-warning">
        <h4>There is no categories!</h4>
    </div>
}

<!-- Delete Modal -->
<div class="modal fade" id="deleteCategoryModal" tabindex="-1" role="dialog" aria-labelledby="deleteCategoryModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirm Deletion</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete <span id="categoryName"></span>?</p>
                <form id="deleteCategoryForm" method="post" action="@Url.Action("Delete", "Category")">
                    <input type="hidden" name="id" id="id" />
                    <div class="modal-footer">
                        <button type="button" class="btn btn-cancel" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-delete">Delete</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.7.1.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.datatables.net/2.1.8/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/2.1.8/js/dataTables.bootstrap5.min.js"></script>
<script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

<script>
    $(document).ready(function () {
        $('#categoryTable').DataTable({
            language: {
                search: "Search by name:",
                searchPlaceholder: "Enter name..."
            }
        });
    });

    function confirmDelete(categoryId, categoryName) {
        $('#categoryName').text(categoryName);
        $('#id').val(categoryId);
        $('#deleteCategoryModal').modal('show');
    }

    document.getElementById('deleteCategoryForm').addEventListener('submit', function (e) {
        e.preventDefault();

        const form = e.target;
        const formData = new FormData(form);

        fetch(form.action, {
            method: 'POST',
            body: formData
        })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert(data.message);
                    location.reload();
                } else {
                    alert(data.message);
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('An error occurred while deleting the category.');
            });
    });
</script>
